apiVersion: v1
kind: Service
metadata:
  name: mermaid-mcp
  namespace: mermaid-mcp
  labels:
    app.kubernetes.io/name: mermaid-mcp
spec:
  type: LoadBalancer
  ports:
    - port: 8100
      targetPort: 8000
      protocol: TCP
      name: sse
  selector:
    app.kubernetes.io/name: mermaid-mcp
