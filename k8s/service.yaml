apiVersion: v1
kind: Service
metadata:
  name: mermaid-mcp
  namespace: mermaid-mcp
  labels:
    app.kubernetes.io/name: mermaid-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: sse
  selector:
    app.kubernetes.io/name: mermaid-mcp
